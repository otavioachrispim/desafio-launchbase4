{% extends "layout.njk" %}

{% block content %}

    <div class="card table-container">
        <a href="/teachers/create">Novo</a>
        <table widht="100%">
            <thead>
                <tr>
                    <th>Professor</th>
                    <th>Matérias</th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody>
                {% for teacher in teachers %}
                    <tr>
                        <td>
                            <span style="background-image: url({{teacher.avatar_url}})"></span>
                            {{teacher.name}}</td>
                        <td>
                            {# {% for service in teacher.services %} #}
                                <span>{{teacher.services}}</span>
                            {# {% endfor %} #}
                        </td>
                        <td>
                            <a href="/teachers/{{teacher.id}}" class="link">ver</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock content %}